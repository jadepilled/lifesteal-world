---
interface Props {
  title: string;
  status?: string;
}

const { title, status = 'READY' } = Astro.props;
---

<header class="page-heading">
  <div class="page-heading__line">
    <h1>{title}</h1>
    <span class="status-chip">[{status}]</span>
  </div>
</header>
